# Python
venv/
venv-mlx-vlm/
__pycache__/
*.pyc
.pytest_cache/

# Sessions (private audio files and transcripts)
sessions/*

# Inbox (input staging area — audio files + rejected duplicates)
inbox/*
!inbox/.gitkeep

# Debug output
sessions/debug/

# IDE
.vscode/
.idea/
.claude/

# Claude context (personal)
CLAUDE.md*.swp
*.swp

# Checkpoints
checkpoints/

# Frontend
ui/node_modules/
ui/dist/

# Speaker profiles (private voice data)
data/speaker_profiles.json

# Experiments (scripts run locally, results are generated output)
experiments/


# EMP per-session HTML visuals (contain transcript text)
emp/results/visuals/

# Private real-name data (family roster); count.py reads it for M9a counting only, never committed
emp/results/pivot-notes.private.json*
