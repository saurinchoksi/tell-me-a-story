# Python
venv/
venv-unified/
__pycache__/
*.pyc
.pytest_cache/

# Sessions (private audio files and transcripts)
sessions/*

# Inbox (input staging area — audio files + rejected duplicates)
inbox/*
!inbox/.gitkeep

# Debug output
sessions/debug/

# IDE
.vscode/
.idea/
.claude/

# Claude context (personal)
CLAUDE.md*.swp
*.swp

# Checkpoints
checkpoints/

# Frontend
ui/node_modules/
ui/dist/

# Speaker profiles (private voice data)
data/speaker_profiles.json

# Family-name roster for detectors (real names) — schema documented in data/name_roster.example.json
data/name_roster.json

# Experiments (scripts run locally, results are generated output)
experiments/


# EMP per-session HTML visuals (contain transcript text)
emp/results/visuals/

# TMAS-46 gap-classify sidecars (Whisper cache + by-ear "what I hear" notes = family speech)
emp/results/gap-classify/*.json

# Private real-name data (family roster); count.py reads it for M9a counting only, never committed
emp/results/pivot-notes.private.json*

# Interactive review scaffolding (regenerable HTML + co-edited working notes)
emp/writeup/*-review.html
emp/writeup/*-notes.json

# Private working doc (Sierra positioning + family names) - never commit
emp/emp.md

# World-classifier cold test: answer key + predictions reference REAL held-out sessions
emp/results/worlds-bench/cold*
